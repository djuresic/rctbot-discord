version: '3.4'

services:
  rctbot:
    image: rctbot
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: rctbot
    ports:
      - 8080:8080
    restart: unless-stopped

networks:
  default:
    external:
      name: rctbot
